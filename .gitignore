# Rust build artifacts
target/
*.wasm
*.wasm.gz

# Node / TypeScript
node_modules/
dist/
*.js.map

# Wallets — NEVER commit private keys
*.pem
*.keystore
*.json.wallet
devnet/provider.pem
devnet/consumer.pem
devnet/wallet.pem

# Deploy outputs (contain addresses but not secrets)
devnet/registry-deploy.json
devnet/escrow-deploy.json
devnet/reputation-deploy.json

# Environment variables
.env
.env.local
.env.devnet
.env.mainnet

# OS / Editor
.DS_Store
.idea/
.vscode/settings.json
*.swp
*.swo
